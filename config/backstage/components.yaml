apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: mood-sensors
  description: Sensors backend component for the DevX Mood app
  tags:
    - spring-boot
    - streaming
    - backend-apis
  annotations:
    'backstage.io/kubernetes-label-selector': 'app.kubernetes.io/part-of=devx-mood-app'
spec:
  type: service
  lifecycle: integration
  owner: dekt
  system: devx-mood-app
  dependsOn: 
    - resource:rds-connector
    - resource:rabbitmq-cluster
